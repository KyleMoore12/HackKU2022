let addNew = document.getElementById("addNew");

addNew.addEventListener("click",() => {
  console.log('add new');
})

// chrome.storage.sync.get("links", ({ color }) => {
//   addNew.style.backgroundColor = color;
// });
